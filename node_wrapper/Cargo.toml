[package]
name = "cryptlog-node"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16.1", default-features = false, features = ["napi3"] }
napi-derive = "2.16.4"
cryptlog = { path = ".." }

[build-dependencies]
napi-build = "2.1.3"
